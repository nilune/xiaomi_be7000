Что я проверил:
1. Передеплоил и настроил static IP.
2. Задеплоил, запулил и запушиш core сервис.

Что не проверил:
1. Деплой, пулинг и пушинг основы / base
2. Деплой, пулинг и пушинг adgaurd
3. Деплой, пулинг и пушинг v2raya
4. Деплой, пулинг и пушинг filebrowser

Проблемы:
- [ ] в целом как работает config.yml. Согласно ему деплоятся все сервисы?
- [ ] как задеплоить только base (базовый startup.sh) без всего остального?
- [ ] почему-то после любой настройки роутер умирает (/etc/resolv.conf становится пустым) и в целом отсутствует интернет. Из-за чего? Вручную перенастройка работает. В чем может быть причина?
- [ ] не хватает доп. команды для настройки статических адресов - кандидатов под настройку статику. То есть это должно быть в своем роде разница между тем, что в leases и в hosts. Также нужно в config.yml иметь список mac адресов и префиксов тех, кто не будет появляться в списке этих кандидатов (чтобы можно было сразу исключить все умные лампочки)
- [ ] очень странный запуск того, что должно быть в контейнере. Почему filebrowser.sh пустой? В целом понятно, потому что он запускается через docker. Но сейчас внутри python кода написаны огромные sh скрипты. Это надо переделать на какую-то более простую настройку с учетом того, что я буду добавлять новые сервисы, которые будут запускаться через docker.
- [ ] пути в логе лучше выводить относительно корня репы, а не в целом абсолютный путь: `/Users/Pakhomov_M/Documents/personal/repositories/router/sync/_System/core/etc -> /mnt/usb-ef8d1024/System/core/etc`
- [ ] если я выключу сервисы, то они физически удаляться с роутера?
- [ ] Удалить бесполезные комментарии по коду. Сделать код клиенто-ориентированным, то есть модифицровать фразы аля "Теперь она позволяет:" в readme.md. В целом весь репозиторий должен выглядеть так, как будто он для конечного пользователя и для него эта инструкция. Если вдруг найдешь ошибки небольшие - поправляй их. Избався от файла DEPLOYER.md (если в нем есть что-то нужное - то перенеси в readme.md). Обнови ARCHITECTURE.md.
- [ ] Если видишь существенные архитектурные проблемы или что-то такое, то записывай это сюда в список задач (составь отдельный список задач), который мы после обсудим