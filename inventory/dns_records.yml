# DNS Records Configuration
# Generates AdGuard DNS rewrites

records:
  # UI Aliases
  router.lan: 192.168.1.1
  router: router.lan
  adguard.lan: 192.168.1.1
  adguard: adguard.lan
  v2raya.lan: 192.168.1.1
  v2raya: v2raya.lan

  # Add custom DNS records below...
